// Title: Leap Year Checker
// Name: Mohd Jeesan Mansoori
// Division: C-2
// Partner: Name: Aditya Chaurasiya
//          Roll no: 28    UIN: 241P029

#include <stdio.h>

int main() {
    int year;

    printf("Enter a year: ");
    scanf("%d", &year);

    if ((year % 4 == 0 && year % 100 != 0) || (year % 400 == 0)) {
        printf("%d is a Leap Year.\n", year);
    } else {
        printf("%d is not a Leap Year.\n", year);
    }

    return 0;
}

    
